services:
  genkitkraft:
    build: .
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      # Uncomment and set credentials to enable authentication
      # - AUTH_CREDENTIALS=admin:changeme
